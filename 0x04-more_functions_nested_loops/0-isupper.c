#include "main.h"
#include <stdio.h>
/**
* checks for uppercase character.
* Returns 1 if @c is uppercase
* Returns 0 otherwise
**/
int main(void)
{
char c;
if(_isupper(c))
{
return (1); 
}
else
return (0);
}
