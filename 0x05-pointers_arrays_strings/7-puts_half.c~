#include "main.h"
/**
 * rev_string - reverse a string
 * @s: string to be reversed
 *
 * Return: 0 or 1
 */
void puts_half(char *str)
{
int s = 0;
int x;
char *z = str;
while (*z != '\0')
{
z++;
s++;
}
for (x = (s/2); x <= (s - 1); x++)
{
_putchar(str[x]);
}
_putchar('\n');
}
