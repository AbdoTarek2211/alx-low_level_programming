#include <stdio.h>
#include <stdlib.h>
 /**
 * main - main function
 * @argc: count of program
 * @argv: number of arguments used
 *
 * Return: always 0.
 */
int main(int argc __attribute__((unused)), char **argv)
{
printf("%s\n", *argv);
return (0);
}
