#include <stdio.h>
#include <stdlib.h>
/**
 * main - main function
 * @argc: count of program
 * @argv: number of arguments used
 *
 * Return: always 0.
 */
int main(int argc, char **argv)
{
int x = 0;
while (x < argc)
{
x++;
(*argv)++;
}
printf("%i\n", number - 1);
return (0);
}
